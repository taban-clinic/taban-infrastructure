# shellcheck shell=bash disable=SC2034
# iran-implant-rescue-institute.ir (clinic-next). Sourced by deploy/lib/common.sh.
UNIT=clinic-next
PORT=7333
ENV_FILES=(.env.production.local)
# /booking talks to Directus; it failing while / works usually means a bad env file.
HEALTH_PATHS=(/ /booking)
KEEP_RELEASES=5
