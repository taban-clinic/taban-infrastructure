# shellcheck shell=bash disable=SC2034
# implantrescue.ir. Sourced by deploy/lib/common.sh.
UNIT=implant-rescue-institute
PORT=7334
ENV_FILES=(.env.production.local)
# The image URL makes a deploy fail (and roll back) when sharp is missing from the
# standalone build. The homepage doesn't emit optimized images yet, so / alone
# wouldn't notice. /doctor/dr-yousefi.png is used by src/app/page.tsx; if that file is
# ever renamed or removed, update this path.
HEALTH_PATHS=(/ "/_next/image?url=%2Fdoctor%2Fdr-yousefi.png&w=384&q=75")
KEEP_RELEASES=5
