# shellcheck shell=bash disable=SC2034,SC2154
# Sourced by services/lib/common.sh; $REPO and $SERVICES_LIVE_ROOT are set by then.
PROJECT=lab
LIVE_DIR="$SERVICES_LIVE_ROOT/services/lab-directus"
COMPOSE_FILES=("$REPO/services/lab-directus/docker-compose.yml")
SERVICES=(postgres directus)
REQUIRE_FILES=(.env)
HTTP_CHECKS=(http://127.0.0.1:61984/server/health)
